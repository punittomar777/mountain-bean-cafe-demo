import { ImageResponse } from "next/og";

// Apple touch icon (iOS home screen / older Safari). Apple masks the corners
// itself, so we render the brand on a filled caramel tile rather than a
// transparent circle — same colour + Lucide "mountain" glyph as the site logo.
export const size = { width: 180, height: 180 };
export const contentType = "image/png";

export default function AppleIcon() {
  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          background: "#a9683a",
        }}
      >
        <svg
          width="104"
          height="104"
          viewBox="0 0 24 24"
          fill="none"
          stroke="#ffffff"
          strokeWidth={2}
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <path d="m8 3 4 8 5-5 5 15H2L8 3z" />
        </svg>
      </div>
    ),
    { ...size }
  );
}
